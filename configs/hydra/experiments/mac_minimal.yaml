# @package _global_

defaults:
  - override /model: small
  - override /data: tiny
  - override /training: mac

training:
  output_dir: ./outputs/mac_minimal_model
  eval_steps: 100
  save_steps: 100
  max_steps: 500