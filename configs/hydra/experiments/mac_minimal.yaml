# @package _global_

defaults:
  - override /model: small
  - override /data: tiny
  - override /training: mac

training:
  output_dir: ./outputs/mac_minimal_model
  num_train_epochs: 2
  eval_steps: 100
  save_per_epoch: 1