# @package _global_

defaults:
  - override /model: large
  - override /data: full
  - override /training: mac

# Mac-specific overrides
training:
  output_dir: ./outputs/mac_minimal_model
  max_steps: 100
  eval_steps: 100
  save_steps: 100